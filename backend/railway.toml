[build]
builder = "railpack"

[deploy]
startCommand = "php artisan config:clear && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=${PORT:-8000}"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
